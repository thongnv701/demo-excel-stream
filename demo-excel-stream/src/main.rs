// This file is kept minimal on purpose.
// The main HTTP server entrypoint now lives in `src/bin/server.rs`.
// Run it with:
//   cargo run --bin server

fn main() {
    eprintln!("Please run the HTTP demo with: cargo run --bin server");
}
